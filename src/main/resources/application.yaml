spring:
  application:
    name: quiz-hub

  datasource:
    url: jdbc:postgresql://localhost:5432/quiz_hub
    username: postgres
    password: 27102005
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect